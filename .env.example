# Google Maps JavaScript API key.
# Vite exposes any VITE_* var to the client bundle — this key WILL be visible
# in browser devtools. Secure it via HTTP-referrer restriction in Google Cloud
# Console (allow your Vercel domain + any custom domain), not by hiding it.
VITE_GMAPS_API_KEY=yAIzaSyCl8QDySbTGG5mlhY_ib-hvIC3hX7JAeg4
